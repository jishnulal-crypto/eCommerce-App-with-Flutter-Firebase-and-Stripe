name: ecommerce_with_flutter_firebase_and_stripe
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.24.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  db_client:
    path: packages/db_client
  payment_client:
    path: packages/payment_client
  cupertino_icons: ^1.0.2
  flutter_staggered_grid_view: ^0.7.0
  equatable: ^2.0.5
  firebase_core: ^3.4.0  # Update firebase_core to a version compatible with cloud_firestore
  cloud_firestore: ^5.4.0
  uuid: ^4.2.2
  flutter_stripe: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter






  flutter_lints: ^2.0.0


flutter:




  uses-material-design: true































